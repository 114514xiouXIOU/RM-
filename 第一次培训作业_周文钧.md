# 第一次培训作业

姓名：＿＿＿＿　　学号：＿＿＿＿　　日期：＿＿＿＿

## 一、开发环境、坐标系约定与重投影原理

### 1.1 开发环境

| 项目 | 内容 |
| --- | --- |
| 操作系统 | ＿＿＿＿（例如：Windows 11 + WSL2 / Ubuntu 24.04） |
| 编译器 | ＿＿＿＿（例如：g++ 16.2.0，Ubuntu 版 / MSYS2 版） |
| 构建工具 | CMake ＿＿＿＿ ＋ Ninja ＿＿＿＿ |
| 版本控制 | Git ＿＿＿＿ |
| 语言标准 | C++17（编译参数 `-std=c++17`） |

安装方式说明：全部通过系统包管理器安装（Ubuntu 下使用 `apt`，未使用 Snap；
Windows 下使用 MSYS2 的 `pacman`）。

### 1.2 坐标系约定

- **外参方向**：世界 → 相机，即 `Pc = R * Pw + t`
- **R**：3×3 旋转矩阵，描述相机朝向
- **t**：3×1 平移向量，描述相机位置，与三维点使用**相同长度单位**
- **相机坐标系**：X 向右、Y 向下、Z 向前（光轴指向 +Z）
- **像素坐标系**：原点在图像左上角，`u` 向右增大，`v` 向下增大

其中 `Pc = (Xc, Yc, Zc)`：`Xc` 为左右方向偏出量，`Yc` 为上下方向偏出量，
`Zc` 为深度（点在相机前方多远）。

**注意**：`t` 并不等于相机在世界坐标系中的位置（相机位置 `C` 满足 `C = -Rᵀt`），
本作业不需要计算 `C`，但需明确二者区别。

### 1.3 重投影原理

采用理想针孔模型，忽略畸变，共三步：

**第 1 步：世界点 → 相机点**

```
Pc = R * Pw + t
```

**第 2 步：深度检查**

若 `Zc <= 0`，该点位于相机平面上或相机后方，应判为无效并停止计算。
若忽略此检查而直接做除法，`Zc < 0` 会使相机后方的点被翻折到画面前方，
得到看似正常但完全错误的像素坐标，且程序不会报错。

**第 3 步：相机点 → 像素**

先做透视除法，投影到距离光心为 1 的归一化平面：

```
x_n = Xc / Zc
y_n = Yc / Zc
```

再经内参线性映射为像素坐标：

```
u = fx * x_n + cx
v = fy * y_n + cy
```

合并即：

```
u = fx * Xc / Zc + cx
v = fy * Yc / Zc + cy
```

**像素欧氏距离**（重投影误差）：

```
error = sqrt( (u - u_obs)^2 + (v - v_obs)^2 )      单位：像素
```

### 1.4 手算算例

数据：`fx = fy = 800`，`cx = 640`，`cy = 360`，`R` 为单位矩阵，`t = (0, 0, -1000)`，
`Pw = (100, 50, 3000)`，观测点 `(680.5, 385.5)`。

| 步骤 | 计算 | 结果 |
| --- | --- | --- |
| 1 | `R * Pw = (100, 50, 3000)`，加 `t` | `Pc = (100, 50, 2000)` |
| 2 | `Zc = 2000 > 0` | 有效 |
| 3 | `x_n = 100 / 2000 = 0.05`，`y_n = 50 / 2000 = 0.025` | 归一化坐标 |
| 3 | `u = 800 × 0.05 + 640 = 680`，`v = 800 × 0.025 + 360 = 380` | `(680, 380)` |
| 4 | `du = -0.5`，`dv = -5.5` | — |
| 4 | `error = sqrt(0.25 + 30.25) = sqrt(30.5)` | `≈ 5.5227` 像素 |

程序输出与之完全一致。

## 二、项目结构、构建运行方法与测试结果

### 2.1 项目结构

```
.
├── CMakeLists.txt
├── include/
│   ├── camera.h            数据类型与坐标变换接口
│   └── reprojection.h      重投影与误差评估接口
├── src/
│   ├── camera.cpp          坐标变换实现
│   ├── reprojection.cpp    重投影与误差实现
│   └── main.cpp            输入解析、逐点计算、结果输出
└── data/
    ├── sample_no_rotation.txt
    └── sample_rotation.txt
```

头文件只放 `struct` 定义与函数声明，源文件放实现；`main.cpp` 不包含数学计算，
只负责输入输出与流程控制。这样划分的依据是"变化的原因不同"：
增加畸变模型只需修改 `reprojection.cpp`，更换旋转表示只需修改 `camera.cpp`。

### 2.2 构建与运行

```bash
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
./build/reproject data/sample_no_rotation.txt
./build/reproject data/sample_rotation.txt
```

构建结果：编译零警告零错误。

### 2.3 测试结果

**测试数据 1**（无旋转）：`fx = fy = 800`，`cx = 640`，`cy = 360`，`t = (0, 0, -1000)`

| 点 | 世界点 Pw | 相机点 Pc | 重投影像素 | 观测像素 | 误差 (px) |
| --- | --- | --- | --- | --- | --- |
| 1 | (0, 0, 1000) | (0, 0, 0) | 无效（`Zc = 0`） | (640, 360) | — |
| 2 | (0, 0, 2000) | (0, 0, 1000) | (640.0000, 360.0000) | (640, 360) | 0.0000 |
| 3 | (100, 50, 3000) | (100, 50, 2000) | (680.0000, 380.0000) | (680.5, 385.5) | 5.5227 |
| 4 | (0, 0, 500) | (0, 0, -500) | 无效（`Zc < 0`） | — | — |

汇总：有效点 2 个，平均误差 2.7613 px，RMSE 3.9051 px，最大误差 5.5227 px。

**测试数据 2**（绕世界 Z 轴 90°，`t = (0, 0, 0)`）：

| 点 | 世界点 Pw | 相机点 Pc | 重投影像素 | 误差 (px) | 画面内 |
| --- | --- | --- | --- | --- | --- |
| 1 | (1000, 0, 1000) | (0, 1000, 1000) | (640.0000, 1160.0000) | 0.1414 | 否 |
| 2 | (0, 1000, 1000) | (-1000, 0, 1000) | (-160.0000, 360.0000) | 0.1414 | 否 |
| 3 | (0, 0, 1000) | (0, 0, 1000) | (640.0000, 360.0000) | 0.1414 | 是 |

汇总：有效点 3 个，平均误差 = RMSE = 0.1414 px。

（在此处插入程序实际运行输出截图）

### 2.4 交互输入模式（在分支上新增的功能）

基础版本只支持从数据文件读入，且运行时不带参数会直接读取标准输入，
但没有任何提示，使用者无法得知输入顺序与每一项的含义。因此在
`feature/interactive-input` 分支上新增了交互模式：不带参数运行时，
程序逐项提示输入内参与外参，随后反复提示输入三维点，
每输入一个点立即给出该点的相机坐标、深度、投影结果与像素误差，
在 `Xw` 处输入 `q` 即可结束并输出统计。

实现上的要点：

- 相机参数录入完成后先打印一次配置摘要，便于在计算前核对参数
- 输入非法内容只提示重试，不中断程序（见 3.1）
- 不带参数识别为交互模式、带参数识别为文件模式，两种模式共用同一套
  `worldToCamera()` 与 `evaluate()` 调用与统计逻辑，因此新增功能不需要改动
  `camera.cpp` 与 `reprojection.cpp`——这也验证了当初按职责拆分文件的价值

### 2.5 误差分析

- **误差来源**：测试数据 1 第 3 点的观测值被人为偏移（`du = -0.5`、`dv = -5.5`），
  理论误差 `sqrt(0.5² + 5.5²) = 5.5227` px，与程序输出一致，说明误差计算链路正确。
- **交叉验证**：另用 Python 独立实现同一套公式，两个测试数据的投影结果与误差
  与 C++ 实现逐位一致，排除语言相关的偶然错误。
- **异常情况覆盖**：测试数据中刻意包含两类异常——点正好落在光心上（`Zc = 0`）、
  点在相机后方（`Zc < 0`），二者均被正确判为无效且不参与统计。
- **精度说明**：所有坐标与内参使用 `double`（约 15～16 位有效数字）。
  若误用 `int`，`50 / 2000` 会被整数除法截断为 `0`，误差将完全错误。
  中间结果不做四舍五入，仅在输出时以 `setprecision(4)` 控制显示位数。

## 三、遇到的问题与解决办法

### 3.1 交互模式下的非法输入会破坏后续所有读取

- **现象**：为程序增加交互输入模式时，最初直接用 `std::cin >> value` 逐项读取。
  测试时故意输入 `abc`，程序没有提示错误，反而把后续所有输入都读错了——
  输入的 5 个点数据全部错位。
- **原因**：`operator>>` 在读到一个无法解析为数字的内容时会置位流的失败状态（failbit），
  此后所有后续读取都会立即失败并返回，程序却仍在继续执行，导致数据整体错位。
  这与"读数失败要重试"的需求是矛盾的。
- **解决**：改为两步走——先用 `std::cin >> token` 把输入读成 `std::string`（任何内容都能读进来），
  再用 `std::stod(token)` 尝试转成 `double`，并捕获 `std::invalid_argument` 与
  `std::out_of_range` 异常。转换失败只打印提示并重新询问，不影响流的后续读取。
  同时记录输入进度所需的状态保存在函数外的变量中，因此重试时不会丢失已输入的内容。

### 3.2 Windows 控制台中文输出乱码

- **现象**：程序输出的中文在 Windows 控制台显示为乱码（如 `鐩告満閰嶇疆`）。
- **原因**：源码字符串为 UTF-8 编码，而 Windows 中文控制台默认按 GBK 解码，
  一个 UTF-8 汉字（3 字节）被按 GBK（2 字节一组）拆读，因此每个汉字变成约 1.5 个乱码字符。
- **解决**：将程序运行时的输出统一改为 ASCII 英文，从根源上避免依赖终端编码；
  源码注释保留中文。`chcp 65001` 只对当前终端窗口有效，换环境仍会复现，故未采用。

### 3.3 CMake 报 `CMAKE_MAKE_PROGRAM is not set`

- **现象**：执行 `cmake -S . -B build` 时报错，无法生成构建文件。
- **原因**：CMake 默认使用 MinGW Makefiles 生成器，而环境中未安装 `mingw32-make`。
- **解决**：改用 Ninja 生成器，即添加 `-G Ninja` 参数（Ninja 已随工具链安装）。

### 3.4 在错误目录执行 `git init`

- **现象**：在用户主目录（`C:\Users\Lenovo`）执行 `git init`，导致仓库建到了错误的路径下；
  随后在项目目录执行 `git status` 时提示
  `fatal: not a git repository (or any of the parent directories): .git`。
- **原因**：`.git` 所在目录即为仓库根目录，决定了哪些文件属于该仓库、`git push` 会上传什么内容。
  执行前没有确认当前工作目录。
- **解决**：删除误建的目录，`cd` 到项目目录后用 `Get-Location` 确认路径，再重新初始化。
  这个错误信息本身也值得记住：`not a git repository` 首先应检查当前目录是否正确。

## 四、代码仓库与提交记录

- **仓库链接**：https://github.com/114514xiouXIOU/RM-
- **PR 链接**：https://github.com/114514xiouXIOU/RM-/pull/1
- **最终提交的 commit hash**：`3bd84f0eba6ecb6f56dfd33bb8f519cc3524e884`
  （`main` 分支上的 merge commit，短 hash `3bd84f0`）

开发流程：

| 提交 | 内容 |
| --- | --- |
| `2327d80` | 在 `main` 上完成基础版本：重投影、异常处理、CMake 构建 |
| `44ffd76` | 在 `feature/interactive-input` 分支上新增交互输入模式 |
| `3bd84f0` | 通过 Pull Request #1 将分支合并回 `main`（merge commit） |

重新克隆后可按第二节的构建方法直接编译运行，已确认验证通过。
