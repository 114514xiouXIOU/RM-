# 第一次培训作业

>写在前面，这次作业中我使用AI的情况
>1.AI帮助我理解了机器视觉中`Pc = R * Pw + t`的含义并给出处理这个问题的方向
>2.AI完成了大部分代码的编写，可以说这次作业的写代码环节非常像vibecoding，像struct这样的东西和向量乘矩阵这样的算法都是一边问AI一边现场去理解的，臣无大肥鱼，无以至今日
>3.AI记录了一些我的提出的问题作为写报告时的素材以及读取了我电脑里开发环境这些乱七八糟的版本号，并在我写完报告时针对作业要求进行了数据的整理和排版润色
>DSH指导了我如何操作去git

## 一、开发环境、坐标系约定与重投影原理

### 1.1 开发环境

| 项目 | 内容 |
| --| --|
| 操作系统 | Windows 11 家庭版中文版 |
| 主开发环境 | MSYS2 ucrt64 |
| 编译器 | g++ 16.2.0 |
| 构建工具 | CMake 4.4.3 ＋ Ninja 1.13.2 |
| 版本控制 | Git 2.55.0.windows.5 |

### 1.2 坐标系约定

外参方向：由世界到相机，即 `Pc = R * Pw + t`
R：3×3 旋转矩阵，描述相机朝向
t：3×1 平移向量，描述相机位置，与三维点使用相同长度单位
相机坐标系：X 向右、Y 向下、Z 向前（光轴指向 +Z）
像素坐标系：原点在图像左上角，`u` 向右增大，`v` 向下增大

其中 `Pc = (Xc, Yc, Zc)`：`Xc` 为左右方向偏出量，`Yc` 为上下方向偏出量，`Zc` 为深度

### 1.3 重投影


第 1 步：由世界点计算相机点
Pc = R * Pw + t
第 2 步：判断处理非正深度
若 `Zc <= 0`，该点位于相机平面上或相机后方，是无效的，但是程序很笨即使是负数它也会接着算下去，得到看似正常但完全错误的像素坐标，且程序不会报错，这让我感觉很头疼，遂请DSH做了一段处理，现在于0的Zc会被拦下来。
第 3 步：由相机点计算像素
先做透视除法，投影到距离光心为 1 的归一化平面：
x_n = Xc / Zc
y_n = Yc / Zc
再经内参线性映射为像素坐标：
u = fx * x_n + cx
v = fy * y_n + cy
合并即：
u = fx * Xc / Zc + cx
v = fy * Yc / Zc + cy

像素欧氏距离：
error = sqrt( (u u_obs)^2 + (v v_obs)^2 )

### 1.4 一些手算算例

数据：`fx = fy = 800`，`cx = 640`，`cy = 360`，`R` 为单位矩阵，`t = (0, 0, -1000)`，
`Pw = (100, 50, 3000)`，观测点 `(680.5, 385.5)`。

| 步骤 | 计算 | 结果 |
| --| --| --|
| 1 | `R * Pw = (100, 50, 3000)`，加 `t` | `Pc = (100, 50, 2000)` |
| 2 | `Zc = 2000 > 0` | 有效 |
| 3 | `x_n = 100 / 2000 = 0.05`，`y_n = 50 / 2000 = 0.025` | 归一化坐标 |
| 3 | `u = 800 × 0.05 + 640 = 680`，`v = 800 × 0.025 + 360 = 380` | `(680, 380)` |
| 4 | `du = -0.5`，`dv = -5.5` | — |
| 4 | `error = sqrt(0.25 + 30.25) = sqrt(30.5)` | `≈ 5.5227` 像素 |

程序输出与之完全一致。

## 二、项目结构、构建运行方法与测试结果

### 2.1 项目结构

.
├── CMakeLists.txt
├── include/
│   ├── camera.h            定义了一些数据类型，坐标变换的头文件
│   └── reprojection.h      重投影与误差评估的头文件
├── src/
│   ├── camera.cpp          实现了坐标变换
│   ├── reprojection.cpp    实现了重投影和误差
│   └── main.cpp            负责输入解析，逐点计算，结果输出
└── data/
    ├── sample_no_rotation.txt 为了用人类看的，比较规整的数来跑程序，我请DSH老师生成了一些易读易算的数据
    └── sample_rotation.txt

### 2.2 构建与运行
运行命令如下：

cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build
./build/reproject data/sample_no_rotation.txt
./build/reproject data/sample_rotation.txt

### 2.3 测试结果

测试数据 1（无旋转）：`fx = fy = 800`，`cx = 640`，`cy = 360`，`t = (0, 0, -1000)`

| 点 | 世界点 Pw | 相机点 Pc | 重投影像素 | 观测像素 | 误差 (px) |
| --| --| --| --| --| --|
| 1 | (0, 0, 1000) | (0, 0, 0) | 无效（`Zc = 0`） | (640, 360) | — |
| 2 | (0, 0, 2000) | (0, 0, 1000) | (640.0000, 360.0000) | (640, 360) | 0.0000 |
| 3 | (100, 50, 3000) | (100, 50, 2000) | (680.0000, 380.0000) | (680.5, 385.5) | 5.5227 |
| 4 | (0, 0, 500) | (0, 0, -500) | 无效（`Zc < 0`） | — | — |

汇总：有效点 2 个，平均误差 2.7613 px，RMSE 3.9051 px，最大误差 5.5227 px。

测试数据 2（绕世界 Z 轴 90°，`t = (0, 0, 0)`）：

| 点 | 世界点 Pw | 相机点 Pc | 重投影像素 | 误差 (px) | 画面内 |
| --| --| --| --| --| --|
| 1 | (1000, 0, 1000) | (0, 1000, 1000) | (640.0000, 1160.0000) | 0.1414 | 否 |
| 2 | (0, 1000, 1000) | (-1000, 0, 1000) | (-160.0000, 360.0000) | 0.1414 | 否 |
| 3 | (0, 0, 1000) | (0, 0, 1000) | (640.0000, 360.0000) | 0.1414 | 是 |

汇总：有效点 3 个，平均误差 = RMSE = 0.1414 px。

### 2.4 交互输入模式（在分支上新增的功能）（DSH写的交互）

基础版本只支持从txt里面读入，且运行时不带参数会直接读取标准输入，而且没有任何提示，使用者无法得知输入顺序与每一项的含义。因此在`feature/interactive-input` 分支上新增了交互模式：不带参数运行时，程序逐项提示输入内参与外参，随后反复提示输入三维点，每输入一个点立即给出该点的相机坐标、深度、投影结果与像素误差，在 `Xw` 处输入 `q` 即可结束并输出统计

### 2.5 误差分析

误差来源：测试数据 1 第 3 点的观测值被我偏移（`du = -0.5`、`dv = -5.5`），理论误差 `sqrt(0.5² + 5.5²) = 5.5227` px，与程序输出一致，说明误差计算链路正确。
异常情况覆盖：DSH给了我一些测试数据，数据中刻意包含两类异常——点正好落在光心上（`Zc = 0`），点在相机后方（`Zc < 0`），二者均被正确判为无效且不参与统计。


## 三、遇到的问题与解决办法

>事实上最大最多的问题在作业初期出现
### 3.00 一开始没理解机器视觉中重投影的概念
现象：刚拿到作业的时候非常懵逼，不知道由世界点到摄像机点的那个公式是什么意思，我又该如何去定义坐标系的朝哪边是正方向，有很多概念是完全不懂的。在这里问AI的时候AI试图画图来解释，但是画的非常抽象导致在这里卡了很长时间。
原因：自己对机器视觉这个领域完全不懂，有待学习。
解决：去知乎上看了节图文，搞懂了。
### 3.0 在C++上试图实现线性代数时受阻
现象：在定义好了各个坐标和矩阵之后我发现自己不会让他们去运算，做不到struct结构下的数据计算（这点让我非常头疼）
原因：咪的C++太烂哩
解决：人非生而吱吱者，孰能无惑，惑而不从大肥鱼，其为惑也，终不解矣。用大肥鱼写了92行的算法，写完之后挨个向我解释，懂个大概了，但是还是不太熟。
### 3.05 阅读代码时受阻
现象：AI写的代码写完我看不懂
原因：vibecoding的通病是ai定义事物的速度超过了我理解这些东西的速度，导致越到后面我越看不懂ai写了个什么屎山出来，到最后不得不让ai逐字解释
解决：让DSH逐个文件夹逐个文件去向我解释各行代码的作用
### 3.1 交互模式下的非法输入会破坏后续所有读取（这点是DSH写的）

现象：新加入程序交互输入模式时，最初直接用 `std::cin >> value` 逐项读取，测试的时侯故意输入 `abc`，程序没有提示错误，反而把后续所有输入都读错了，导致一错再错，输入的 5 个点数据全部错位。
原因：DSH自己debug...`operator>>` 在读到一个无法解析为数字的内容时会置位流的失败状态（failbit），此后所有后续读取都会立即失败并返回，程序却仍在继续执行，导致数据整体错位。
解决：改为两步走——先用 `std::cin >> token` 把输入读成 `std::string`（任何内容都能读进来），再用 `std::stod(token)` 尝试转成 `double`，并捕获 `std::invalid_argument` 与`std::out_of_range` 异常。转换失败只打印提示并重新询问，不影响流的后续读取。同时记录输入进度所需的状态保存在函数外的变量中，因此重试时不会丢失已输入的内容。

### 3.2 Windows 控制台中文输出乱码

现象：程序输出的中文在 Windows 控制台显示为乱码
原因：UTF-8 编码和GBK 解码混用了
解决：程序运行时的输出统一改为 ASCII 英文。当时还使用了`chcp 65001` 但是只对当前终端窗口有效，换环境仍会复现

### 3.3 使用CMake跑代码时报错

现象：我在执行 `cmake -S . -B build` 时报错`CMAKE_MAKE_PROGRAM is not set`
原因：环境中未安装 `mingw32-make`装完就好了
解决：下一个Ninja 生成器

### 3.4 在错误目录执行 `git init`

现象：我第一回仓库建错了，在用户主目录（`C:\Users\Lenovo`）执行 `git init`，导致仓库建到了错误的路径下；随后在项目目录执行 `git status` 时提示
`fatal: not a git repository (or any of the parent directories): .git`。
原因：`.git` 所在目录即为仓库根目录，决定了哪些文件属于该仓库、`git push` 会上传什么内容。我在执行前没有确认当前工作目录。
解决：删除误建的目录，`cd` 到项目目录后用 `Get-Location` 确认路径，再重新初始化。
这个错误信息本身也值得记住：`not a git repository` 首先应检查当前目录是否正确。

### 3.5 git push时无法连接 github.com

现象：`git push` 报错`fatal: unable to access 'https://github.com/...': Failed to connect to github.com:443 after 21153 ms: Could not connect to server`但同一台机器上用浏览器可以正常访问 GitHub。
原因：powershall不走代理，只能全局
解决：将代理切换为全局模式后推送成功。

## 四、代码仓库与提交记录

仓库链接：https://github.com/114514xiouXIOU/RM-
PR 链接：https://github.com/114514xiouXIOU/RM-/pull/1
最终提交的 commit hash：`6b7191c70859a9ca1f3db1e83cecf8ce30f023c2`
